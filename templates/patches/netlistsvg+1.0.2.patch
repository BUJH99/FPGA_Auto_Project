diff --git a/node_modules/netlistsvg/built/Cell.js b/node_modules/netlistsvg/built/Cell.js
index 8259998..346b63d 100644
--- a/node_modules/netlistsvg/built/Cell.js
+++ b/node_modules/netlistsvg/built/Cell.js
@@ -187,11 +187,22 @@ var Cell = /** @class */ (function () {
             type === 'generic') {
             var inTemplates_1 = Skin_1.default.getPortsWithPrefix(template, 'in');
             var outTemplates_1 = Skin_1.default.getPortsWithPrefix(template, 'out');
+            var shifts_1 = this.getGenericPortShifts(template);
             var inPorts = this.inputPorts.map(function (ip, i) {
-                return ip.getGenericElkPort(i, inTemplates_1, 'in');
+                var p = ip.getGenericElkPort(i, inTemplates_1, 'in');
+                p.y += shifts_1.inShift;
+                if (p.labels) {
+                    p.labels.forEach(function (l) { return l.y += shifts_1.inShift; });
+                }
+                return p;
             });
             var outPorts = this.outputPorts.map(function (op, i) {
-                return op.getGenericElkPort(i, outTemplates_1, 'out');
+                var p = op.getGenericElkPort(i, outTemplates_1, 'out');
+                p.y += shifts_1.outShift;
+                if (p.labels) {
+                    p.labels.forEach(function (l) { return l.y += shifts_1.outShift; });
+                }
+                return p;
             });
             var cell = {
                 id: this.key,
@@ -238,6 +249,7 @@ var Cell = /** @class */ (function () {
         return ret;
     };
     Cell.prototype.render = function (cell) {
+        var _this = this;
         var template = this.getTemplate();
         var tempclone = clone(template);
         for (var _i = 0, _a = cell.labels; _i < _a.length; _i++) {
@@ -294,6 +306,7 @@ var Cell = /** @class */ (function () {
             var outPorts_2 = Skin_1.default.getPortsWithPrefix(template, 'out');
             var outgap_1 = Number(outPorts_2[1][1]['s:y']) - Number(outPorts_2[0][1]['s:y']);
             var outstartY_1 = Number(outPorts_2[0][1]['s:y']);
+            var shifts_2 = this.getGenericPortShifts(template);
             tempclone.pop();
             tempclone.pop();
             tempclone.pop();
@@ -302,15 +315,18 @@ var Cell = /** @class */ (function () {
                 var portClone = clone(inPorts_2[0]);
                 portClone[portClone.length - 1][2] = port.Key;
                 portClone[1].transform = 'translate(' + inPorts_2[1][1]['s:x'] + ','
-                    + (instartY_1 + i * ingap_1) + ')';
+                    + (instartY_1 + i * ingap_1 + shifts_2.inShift) + ')';
                 portClone[1].id = 'port_' + port.parentNode.Key + '~' + port.Key;
                 tempclone.push(portClone);
             });
             this.outputPorts.forEach(function (port, i) {
                 var portClone = clone(outPorts_2[0]);
+                if (!_this.shouldRenderGenericOutputArrow(port)) {
+                    _this.stripGenericOutputArrow(portClone);
+                }
                 portClone[portClone.length - 1][2] = port.Key;
                 portClone[1].transform = 'translate(' + outPorts_2[1][1]['s:x'] + ','
-                    + (outstartY_1 + i * outgap_1) + ')';
+                    + (outstartY_1 + i * outgap_1 + shifts_2.outShift) + ')';
                 portClone[1].id = 'port_' + port.parentNode.Key + '~' + port.Key;
                 tempclone.push(portClone);
             });
@@ -369,6 +385,51 @@ var Cell = /** @class */ (function () {
         }
         return Number(template[1]['s:height']);
     };
+    Cell.prototype.getPortGap = function (templatePorts) {
+        if (templatePorts.length > 1) {
+            return Number(templatePorts[1][1]['s:y']) - Number(templatePorts[0][1]['s:y']);
+        }
+        return 20;
+    };
+    Cell.prototype.getGenericPortShifts = function (template) {
+        if (template[1]['s:type'] !== 'generic') {
+            return { inShift: 0, outShift: 0 };
+        }
+        var inTemplates = Skin_1.default.getPortsWithPrefix(template, 'in');
+        var outTemplates = Skin_1.default.getPortsWithPrefix(template, 'out');
+        var inCount = this.inputPorts.length;
+        var outCount = this.outputPorts.length;
+        if (inCount <= 0 || outCount <= 0 || inCount === outCount) {
+            return { inShift: 0, outShift: 0 };
+        }
+        var inGap = this.getPortGap(inTemplates);
+        var outGap = this.getPortGap(outTemplates);
+        if (inCount > outCount) {
+            return { inShift: 0, outShift: ((inCount - outCount) * outGap) / 2 };
+        }
+        return { inShift: ((outCount - inCount) * inGap) / 2, outShift: 0 };
+    };
+    Cell.prototype.shouldRenderGenericOutputArrow = function (port) {
+        var wire = port.wire;
+        if (!wire || !wire.riders || wire.riders.length === 0) {
+            return true;
+        }
+        return wire.riders.some(function (rider) {
+            if (!rider || !rider.parentNode) {
+                return false;
+            }
+            return rider.parentNode.Type !== '$_outputExt_';
+        });
+    };
+    Cell.prototype.stripGenericOutputArrow = function (portClone) {
+        for (var i = 2; i < portClone.length; i++) {
+            var node = portClone[i];
+            if (node && node[0] === 'path') {
+                portClone.splice(i, 1);
+                return;
+            }
+        }
+    };
     return Cell;
 }());
 exports.default = Cell;
diff --git a/node_modules/netlistsvg/lib/Cell.ts b/node_modules/netlistsvg/lib/Cell.ts
index a59ebd4..4d3d220 100644
--- a/node_modules/netlistsvg/lib/Cell.ts
+++ b/node_modules/netlistsvg/lib/Cell.ts
@@ -199,10 +199,23 @@ export default class Cell {
             type === 'generic') {
             const inTemplates: any[] = Skin.getPortsWithPrefix(template, 'in');
             const outTemplates: any[] = Skin.getPortsWithPrefix(template, 'out');
-            const inPorts = this.inputPorts.map((ip, i) =>
-                ip.getGenericElkPort(i, inTemplates, 'in'));
-            const outPorts = this.outputPorts.map((op, i) =>
-                op.getGenericElkPort(i, outTemplates, 'out'));
+            const shifts = this.getGenericPortShifts(template);
+            const inPorts = this.inputPorts.map((ip, i) => {
+                const p = ip.getGenericElkPort(i, inTemplates, 'in');
+                p.y += shifts.inShift;
+                if (p.labels) {
+                    p.labels.forEach((l) => l.y += shifts.inShift);
+                }
+                return p;
+            });
+            const outPorts = this.outputPorts.map((op, i) => {
+                const p = op.getGenericElkPort(i, outTemplates, 'out');
+                p.y += shifts.outShift;
+                if (p.labels) {
+                    p.labels.forEach((l) => l.y += shifts.outShift);
+                }
+                return p;
+            });
             const cell: ElkModel.Cell = {
                 id: this.key,
                 width: Number(template[1]['s:width']),
@@ -303,6 +316,7 @@ export default class Cell {
             const outPorts = Skin.getPortsWithPrefix(template, 'out');
             const outgap = Number(outPorts[1][1]['s:y']) - Number(outPorts[0][1]['s:y']);
             const outstartY = Number(outPorts[0][1]['s:y']);
+            const shifts = this.getGenericPortShifts(template);
             tempclone.pop();
             tempclone.pop();
             tempclone.pop();
@@ -311,15 +325,18 @@ export default class Cell {
                 const portClone = clone(inPorts[0]);
                 portClone[portClone.length - 1][2] = port.Key;
                 portClone[1].transform = 'translate(' + inPorts[1][1]['s:x'] + ','
-                    + (instartY + i * ingap) + ')';
+                    + (instartY + i * ingap + shifts.inShift) + ')';
                 portClone[1].id = 'port_' + port.parentNode.Key + '~' + port.Key;
                 tempclone.push(portClone);
             });
             this.outputPorts.forEach((port, i) => {
                 const portClone = clone(outPorts[0]);
+                if (!this.shouldRenderGenericOutputArrow(port)) {
+                    this.stripGenericOutputArrow(portClone);
+                }
                 portClone[portClone.length - 1][2] = port.Key;
                 portClone[1].transform = 'translate(' + outPorts[1][1]['s:x'] + ','
-                    + (outstartY + i * outgap) + ')';
+                    + (outstartY + i * outgap + shifts.outShift) + ')';
                 portClone[1].id = 'port_' + port.parentNode.Key + '~' + port.Key;
                 tempclone.push(portClone);
             });
@@ -377,6 +394,55 @@ export default class Cell {
         return Number(template[1]['s:height']);
     }
 
+    private getPortGap(templatePorts: any[]): number {
+        if (templatePorts.length > 1) {
+            return Number(templatePorts[1][1]['s:y']) - Number(templatePorts[0][1]['s:y']);
+        }
+        return 20;
+    }
+
+    private getGenericPortShifts(template: any) {
+        if (template[1]['s:type'] !== 'generic') {
+            return { inShift: 0, outShift: 0 };
+        }
+        const inTemplates = Skin.getPortsWithPrefix(template, 'in');
+        const outTemplates = Skin.getPortsWithPrefix(template, 'out');
+        const inCount = this.inputPorts.length;
+        const outCount = this.outputPorts.length;
+        if (inCount <= 0 || outCount <= 0 || inCount === outCount) {
+            return { inShift: 0, outShift: 0 };
+        }
+        const inGap = this.getPortGap(inTemplates);
+        const outGap = this.getPortGap(outTemplates);
+        if (inCount > outCount) {
+            return { inShift: 0, outShift: ((inCount - outCount) * outGap) / 2 };
+        }
+        return { inShift: ((outCount - inCount) * inGap) / 2, outShift: 0 };
+    }
+
+    private shouldRenderGenericOutputArrow(port: Port): boolean {
+        const wire = (port as any).wire;
+        if (!wire || !wire.riders || wire.riders.length === 0) {
+            return true;
+        }
+        return wire.riders.some((rider) => {
+            if (!rider || !rider.parentNode) {
+                return false;
+            }
+            return rider.parentNode.Type !== '$_outputExt_';
+        });
+    }
+
+    private stripGenericOutputArrow(portClone: any): void {
+        for (let i = 2; i < portClone.length; i++) {
+            const node = portClone[i];
+            if (node && node[0] === 'path') {
+                portClone.splice(i, 1);
+                return;
+            }
+        }
+    }
+
 }
 
 function setGenericSize(tempclone, height) {
